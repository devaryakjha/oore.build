# Oore Server Configuration

# Database URL (SQLite)
DATABASE_URL=sqlite:oore.db

# Base URL for the server (used for generating webhook URLs)
OORE_BASE_URL=http://localhost:8080

# CORS: Restrict dashboard origin (optional, defaults to allowing all in dev)
# OORE_DASHBOARD_ORIGIN=http://localhost:3000

# Development mode: relaxes HTTPS requirements for local development
# OORE_DEV_MODE=true

# Demo mode: provides fake/mock data for UI testing without real GitHub/GitLab connections
# When enabled, all API endpoints return simulated data (repositories, builds, logs, etc.)
# OORE_DEMO_MODE=true

# GitHub App Configuration (all required for GitHub integration)
# GITHUB_APP_ID=123456
# GITHUB_PRIVATE_KEY_PATH=/path/to/private-key.pem
# GITHUB_WEBHOOK_SECRET=your-webhook-secret

# GitLab Configuration
# GITLAB_SERVER_PEPPER=random-32-char-secret-for-hmac
# GITLAB_API_BASE_URL=https://gitlab.com
