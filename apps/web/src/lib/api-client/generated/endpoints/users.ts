/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Oore CI API
 * REST API for Oore CI — a self-hosted, Flutter-first mobile CI and internal app distribution platform.
 *
 * The backend daemon (`oored`) exposes this API on the configured listen address. All endpoints under `/v1/` use JSON request/response bodies unless noted otherwise.
 *
 * ## Authentication
 *
 * - **Setup endpoints** (`/v1/setup/*`) are token-gated by a bootstrap session token and auto-disabled after setup completes.
 * - **Auth endpoints** (`/v1/auth/*`) support Local Only login, single-use local recovery in Ready External Access mode, and configured OIDC or trusted-proxy flows.
 * - **Protected API endpoints** declare their session, API, or runner bearer-token requirement individually.
 * - **Callbacks, webhooks, metrics, readiness, and signed-token delivery routes** use their route-specific public, state, signature, or token contract instead of a global bearer requirement.
 *
 * ## Base URL
 *
 * Since Oore CI is self-hosted, the base URL is your daemon's listen address (e.g. `http://localhost:8787`).
 * OpenAPI spec version: 1.0.0
 */
import type {
  InviteUserRequest,
  InviteUserResponse,
  ListUsersParams,
  ListUsersResponse,
  OkResponse,
  ReEnableUserResponse,
  UpdateUserRoleRequest,
  UpdateUserRoleResponse,
  UserProfileResponse,
} from '../models'

import { ooreRequest } from '../../transport.ts'

export const getListUsersUrl = (params?: ListUsersParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/v1/users?${stringifiedParams}`
    : `/v1/users`
}

/**
 * Returns all users. Requires `owner` or `admin` role.
 * @summary List users
 */
export const listUsers = async (
  params?: ListUsersParams,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<ListUsersResponse> => {
  return ooreRequest<ListUsersResponse>(getListUsersUrl(params), {
    ...options,
    method: 'GET',
  })
}

export const getInviteUserUrl = () => {
  return `/v1/users/invite`
}

/**
 * Creates a new user with `invited` status. Requires `owner` or `admin` role.
 * @summary Invite user
 */
export const inviteUser = async (
  inviteUserRequest: InviteUserRequest,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<InviteUserResponse> => {
  return ooreRequest<InviteUserResponse>(getInviteUserUrl(), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(inviteUserRequest),
  })
}

export const getGetMeUrl = () => {
  return `/v1/users/me`
}

/**
 * Returns the profile of the currently authenticated user.
 * @summary Get current user
 */
export const getMe = async (
  options?: Parameters<typeof ooreRequest>[1],
): Promise<UserProfileResponse> => {
  return ooreRequest<UserProfileResponse>(getGetMeUrl(), {
    ...options,
    method: 'GET',
  })
}

export const getDeleteUserUrl = (userId: string) => {
  return `/v1/users/${userId}`
}

/**
 * Soft-deletes a user. Requires `owner` or `admin` role.
 * @summary Delete user
 */
export const deleteUser = async (
  userId: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<OkResponse> => {
  return ooreRequest<OkResponse>(getDeleteUserUrl(userId), {
    ...options,
    method: 'DELETE',
  })
}

export const getReEnableUserUrl = (userId: string) => {
  return `/v1/users/${userId}/enable`
}

/**
 * Re-enables a previously disabled user.
 * @summary Re-enable user
 */
export const reEnableUser = async (
  userId: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<ReEnableUserResponse> => {
  return ooreRequest<ReEnableUserResponse>(getReEnableUserUrl(userId), {
    ...options,
    method: 'POST',
  })
}

export const getUpdateUserRoleUrl = (userId: string) => {
  return `/v1/users/${userId}/role`
}

/**
 * Changes a user's role. Requires `owner` or `admin` role.
 * @summary Update user role
 */
export const updateUserRole = async (
  userId: string,
  updateUserRoleRequest: UpdateUserRoleRequest,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<UpdateUserRoleResponse> => {
  return ooreRequest<UpdateUserRoleResponse>(getUpdateUserRoleUrl(userId), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(updateUserRoleRequest),
  })
}
