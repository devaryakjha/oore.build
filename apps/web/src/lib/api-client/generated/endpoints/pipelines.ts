/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Oore CI API
 * REST API for Oore CI — a self-hosted, Flutter-first mobile CI and internal app distribution platform.
 *
 * The backend daemon (`oored`) exposes this API on the configured listen address. All endpoints under `/v1/` use JSON request/response bodies unless noted otherwise.
 *
 * ## Authentication
 *
 * - **Setup endpoints** (`/v1/setup/*`) are token-gated by a bootstrap session token and auto-disabled after setup completes.
 * - **Auth endpoints** (`/v1/auth/*`) support Local Only login, single-use local recovery in Ready External Access mode, and configured OIDC or trusted-proxy flows.
 * - **Protected API endpoints** declare their session, API, or runner bearer-token requirement individually.
 * - **Callbacks, webhooks, metrics, readiness, and signed-token delivery routes** use their route-specific public, state, signature, or token contract instead of a global bearer requirement.
 *
 * ## Base URL
 *
 * Since Oore CI is self-hosted, the base URL is your daemon's listen address (e.g. `http://localhost:8787`).
 * OpenAPI spec version: 1.0.0
 */
import type {
  CreatePipelineRequest,
  CreatePipelineResponse,
  DiscoverRepositoryWorkflowsParams,
  DiscoverRepositoryWorkflowsResponse,
  ListPipelinesParams,
  ListPipelinesResponse,
  OkResponse,
  PipelineDetailResponse,
  UpdatePipelineRequest,
  ValidatePipelineRequest,
  ValidatePipelineResponse,
} from '../models'

import { ooreRequest } from '../../transport.ts'

export const getValidatePipelineUrl = () => {
  return `/v1/pipelines/validate`
}

/**
 * Validates a pipeline configuration without persisting it.
 * @summary Validate pipeline config
 */
export const validatePipeline = async (
  validatePipelineRequest: ValidatePipelineRequest,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<ValidatePipelineResponse> => {
  return ooreRequest<ValidatePipelineResponse>(getValidatePipelineUrl(), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(validatePipelineRequest),
  })
}

export const getGetPipelineUrl = (pipelineId: string) => {
  return `/v1/pipelines/${pipelineId}`
}

/**
 * @summary Get pipeline detail
 */
export const getPipeline = async (
  pipelineId: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<PipelineDetailResponse> => {
  return ooreRequest<PipelineDetailResponse>(getGetPipelineUrl(pipelineId), {
    ...options,
    method: 'GET',
  })
}

export const getDeletePipelineUrl = (pipelineId: string) => {
  return `/v1/pipelines/${pipelineId}`
}

/**
 * @summary Delete pipeline
 */
export const deletePipeline = async (
  pipelineId: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<OkResponse> => {
  return ooreRequest<OkResponse>(getDeletePipelineUrl(pipelineId), {
    ...options,
    method: 'DELETE',
  })
}

export const getUpdatePipelineUrl = (pipelineId: string) => {
  return `/v1/pipelines/${pipelineId}`
}

/**
 * @summary Update pipeline
 */
export const updatePipeline = async (
  pipelineId: string,
  updatePipelineRequest: UpdatePipelineRequest,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<CreatePipelineResponse> => {
  return ooreRequest<CreatePipelineResponse>(getUpdatePipelineUrl(pipelineId), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(updatePipelineRequest),
  })
}

export const getListPipelinesUrl = (
  projectId: string,
  params?: ListPipelinesParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/v1/projects/${projectId}/pipelines?${stringifiedParams}`
    : `/v1/projects/${projectId}/pipelines`
}

/**
 * @summary List pipelines
 */
export const listPipelines = async (
  projectId: string,
  params?: ListPipelinesParams,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<ListPipelinesResponse> => {
  return ooreRequest<ListPipelinesResponse>(
    getListPipelinesUrl(projectId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getCreatePipelineUrl = (projectId: string) => {
  return `/v1/projects/${projectId}/pipelines`
}

/**
 * @summary Create pipeline
 */
export const createPipeline = async (
  projectId: string,
  createPipelineRequest: CreatePipelineRequest,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<CreatePipelineResponse> => {
  return ooreRequest<CreatePipelineResponse>(getCreatePipelineUrl(projectId), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(createPipelineRequest),
  })
}

export const getDiscoverRepositoryWorkflowsUrl = (
  projectId: string,
  params?: DiscoverRepositoryWorkflowsParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/v1/projects/${projectId}/repository-workflows?${stringifiedParams}`
    : `/v1/projects/${projectId}/repository-workflows`
}

/**
 * Reads supported Oore workflow files from the linked repository without
 * modifying it. The response contains validated behavior but never raw YAML,
 * environment values, or source credentials.
 * @summary Discover repository-owned workflows
 */
export const discoverRepositoryWorkflows = async (
  projectId: string,
  params?: DiscoverRepositoryWorkflowsParams,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<DiscoverRepositoryWorkflowsResponse> => {
  return ooreRequest<DiscoverRepositoryWorkflowsResponse>(
    getDiscoverRepositoryWorkflowsUrl(projectId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}
