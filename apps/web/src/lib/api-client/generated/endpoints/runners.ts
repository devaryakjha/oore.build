/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Oore CI API
 * REST API for Oore CI — a self-hosted, Flutter-first mobile CI and internal app distribution platform.
 *
 * The backend daemon (`oored`) exposes this API on the configured listen address. All endpoints under `/v1/` use JSON request/response bodies unless noted otherwise.
 *
 * ## Authentication
 *
 * - **Setup endpoints** (`/v1/setup/*`) are token-gated by a bootstrap session token and auto-disabled after setup completes.
 * - **Auth endpoints** (`/v1/auth/*`) support Local Only login, single-use local recovery in Ready External Access mode, and configured OIDC or trusted-proxy flows.
 * - **Protected API endpoints** declare their session, API, or runner bearer-token requirement individually.
 * - **Callbacks, webhooks, metrics, readiness, and signed-token delivery routes** use their route-specific public, state, signature, or token contract instead of a global bearer requirement.
 *
 * ## Base URL
 *
 * Since Oore CI is self-hosted, the base URL is your daemon's listen address (e.g. `http://localhost:8787`).
 * OpenAPI spec version: 1.0.0
 */
import type {
  BinaryPayload,
  BuildDetailResponse,
  ClaimJobRequest,
  ClaimJobResponse,
  GitlabCheckoutDiscoveryParams,
  JobStatusResponse,
  ListRunnersParams,
  ListRunnersResponse,
  RegisterRunnerRequest,
  RegisterRunnerResponse,
  RunnerHeartbeatRequest,
  UpdateJobStatusRequest,
  UpdateRunnerRequest,
  UpdateRunnerResponse,
} from '../models'

import { ooreRequest } from '../../transport.ts'

export const getListRunnersUrl = (params?: ListRunnersParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/v1/runners?${stringifiedParams}`
    : `/v1/runners`
}

/**
 * @summary List runners
 */
export const listRunners = async (
  params?: ListRunnersParams,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<ListRunnersResponse> => {
  return ooreRequest<ListRunnersResponse>(getListRunnersUrl(params), {
    ...options,
    method: 'GET',
  })
}

export const getRegisterRunnerUrl = () => {
  return `/v1/runners/register`
}

/**
 * Registers a new build runner and returns a runner authentication token.
 * @summary Register runner
 */
export const registerRunner = async (
  registerRunnerRequest: RegisterRunnerRequest,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<RegisterRunnerResponse> => {
  return ooreRequest<RegisterRunnerResponse>(getRegisterRunnerUrl(), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(registerRunnerRequest),
  })
}

export const getGetRunnerUrl = (runnerId: string) => {
  return `/v1/runners/${runnerId}`
}

/**
 * Retrieves a single runner by ID, including its current status and health information.
 * @summary Get runner
 */
export const getRunner = async (
  runnerId: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<UpdateRunnerResponse> => {
  return ooreRequest<UpdateRunnerResponse>(getGetRunnerUrl(runnerId), {
    ...options,
    method: 'GET',
  })
}

export const getDeleteRunnerUrl = (runnerId: string) => {
  return `/v1/runners/${runnerId}`
}

/**
 * Deletes a user-registered runner that has never claimed a build.
 * @summary Delete runner
 */
export const deleteRunner = async (
  runnerId: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<void> => {
  return ooreRequest<void>(getDeleteRunnerUrl(runnerId), {
    ...options,
    method: 'DELETE',
  })
}

export const getUpdateRunnerUrl = (runnerId: string) => {
  return `/v1/runners/${runnerId}`
}

/**
 * @summary Update runner
 */
export const updateRunner = async (
  runnerId: string,
  updateRunnerRequest: UpdateRunnerRequest,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<UpdateRunnerResponse> => {
  return ooreRequest<UpdateRunnerResponse>(getUpdateRunnerUrl(runnerId), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(updateRunnerRequest),
  })
}

export const getClaimJobUrl = (runnerId: string) => {
  return `/v1/runners/${runnerId}/claim`
}

/**
 * Runner claims the next queued build. Returns `null` job if no builds are available.
 * @summary Claim job
 */
export const claimJob = async (
  runnerId: string,
  claimJobRequest: ClaimJobRequest,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<ClaimJobResponse> => {
  return ooreRequest<ClaimJobResponse>(getClaimJobUrl(runnerId), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(claimJobRequest),
  })
}

export const getRunnerHeartbeatUrl = (runnerId: string) => {
  return `/v1/runners/${runnerId}/heartbeat`
}

/**
 * Sends a heartbeat from a runner to report its status. Authenticated via runner token.
 * @summary Runner heartbeat
 */
export const runnerHeartbeat = async (
  runnerId: string,
  runnerHeartbeatRequest: RunnerHeartbeatRequest,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<void> => {
  return ooreRequest<void>(getRunnerHeartbeatUrl(runnerId), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(runnerHeartbeatRequest),
  })
}

export const getGetJobStatusUrl = (runnerId: string, jobId: string) => {
  return `/v1/runners/${runnerId}/jobs/${jobId}`
}

/**
 * Runner checks the current status of its assigned build.
 * @summary Get job status
 */
export const getJobStatus = async (
  runnerId: string,
  jobId: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<JobStatusResponse> => {
  return ooreRequest<JobStatusResponse>(getGetJobStatusUrl(runnerId, jobId), {
    ...options,
    method: 'GET',
  })
}

export const getGitlabCheckoutDiscoveryUrl = (
  runnerId: string,
  jobId: string,
  gitPath: string,
  params: GitlabCheckoutDiscoveryParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/v1/runners/${runnerId}/jobs/${jobId}/gitlab/${gitPath}?${stringifiedParams}`
    : `/v1/runners/${runnerId}/jobs/${jobId}/gitlab/${gitPath}`
}

/**
 * Internal Git smart-HTTP endpoint. The runner token is accepted only for
 * the repository assigned to this job; GitLab credentials remain server-side.
 * @summary Discover a private GitLab repository for checkout
 */
export const gitlabCheckoutDiscovery = async (
  runnerId: string,
  jobId: string,
  gitPath: string,
  params: GitlabCheckoutDiscoveryParams,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<BinaryPayload> => {
  return ooreRequest<BinaryPayload>(
    getGitlabCheckoutDiscoveryUrl(runnerId, jobId, gitPath, params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGitlabCheckoutUploadPackUrl = (
  runnerId: string,
  jobId: string,
  gitPath: string,
) => {
  return `/v1/runners/${runnerId}/jobs/${jobId}/gitlab/${gitPath}`
}

/**
 * @summary Stream a private GitLab upload-pack request
 */
export const gitlabCheckoutUploadPack = async (
  runnerId: string,
  jobId: string,
  gitPath: string,
  binaryPayload: BinaryPayload,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<BinaryPayload> => {
  return ooreRequest<BinaryPayload>(
    getGitlabCheckoutUploadPackUrl(runnerId, jobId, gitPath),
    {
      ...options,
      method: 'POST',
      headers: {
        'Content-Type': 'application/x-git-upload-pack-request',
        ...options?.headers,
      },
      body: binaryPayload,
    },
  )
}

export const getUpdateJobStatusUrl = (runnerId: string, jobId: string) => {
  return `/v1/runners/${runnerId}/jobs/${jobId}/status`
}

/**
 * Runner reports build status transitions and step results.
 * @summary Update job status
 */
export const updateJobStatus = async (
  runnerId: string,
  jobId: string,
  updateJobStatusRequest: UpdateJobStatusRequest,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<BuildDetailResponse> => {
  return ooreRequest<BuildDetailResponse>(
    getUpdateJobStatusUrl(runnerId, jobId),
    {
      ...options,
      method: 'POST',
      headers: { 'Content-Type': 'application/json', ...options?.headers },
      body: JSON.stringify(updateJobStatusRequest),
    },
  )
}
