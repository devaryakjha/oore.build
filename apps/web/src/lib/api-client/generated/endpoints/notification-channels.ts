/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Oore CI API
 * REST API for Oore CI — a self-hosted, Flutter-first mobile CI and internal app distribution platform.
 *
 * The backend daemon (`oored`) exposes this API on the configured listen address. All endpoints under `/v1/` use JSON request/response bodies unless noted otherwise.
 *
 * ## Authentication
 *
 * - **Setup endpoints** (`/v1/setup/*`) are token-gated by a bootstrap session token and auto-disabled after setup completes.
 * - **Auth endpoints** (`/v1/auth/*`) support Local Only login, single-use local recovery in Ready External Access mode, and configured OIDC or trusted-proxy flows.
 * - **Protected API endpoints** declare their session, API, or runner bearer-token requirement individually.
 * - **Callbacks, webhooks, metrics, readiness, and signed-token delivery routes** use their route-specific public, state, signature, or token contract instead of a global bearer requirement.
 *
 * ## Base URL
 *
 * Since Oore CI is self-hosted, the base URL is your daemon's listen address (e.g. `http://localhost:8787`).
 * OpenAPI spec version: 1.0.0
 */
import type {
  CreateNotificationChannelRequest,
  DeleteNotificationChannelResponse,
  ListNotificationChannelsParams,
  ListNotificationChannelsResponse,
  ListNotificationDeliveriesResponse,
  NotificationChannelResponse,
  TestNotificationChannelResponse,
  UpdateNotificationChannelRequest,
} from '../models'

import { ooreRequest } from '../../transport.ts'

export const getListNotificationChannelsUrl = (
  params?: ListNotificationChannelsParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/v1/settings/notification-channels?${stringifiedParams}`
    : `/v1/settings/notification-channels`
}

/**
 * Returns all configured notification channels.
 * @summary List notification channels
 */
export const listNotificationChannels = async (
  params?: ListNotificationChannelsParams,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<ListNotificationChannelsResponse> => {
  return ooreRequest<ListNotificationChannelsResponse>(
    getListNotificationChannelsUrl(params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getCreateNotificationChannelUrl = () => {
  return `/v1/settings/notification-channels`
}

/**
 * Creates a new webhook or Mattermost notification channel.
 * @summary Create notification channel
 */
export const createNotificationChannel = async (
  createNotificationChannelRequest: CreateNotificationChannelRequest,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<NotificationChannelResponse> => {
  return ooreRequest<NotificationChannelResponse>(
    getCreateNotificationChannelUrl(),
    {
      ...options,
      method: 'POST',
      headers: { 'Content-Type': 'application/json', ...options?.headers },
      body: JSON.stringify(createNotificationChannelRequest),
    },
  )
}

export const getGetNotificationChannelUrl = (id: string) => {
  return `/v1/settings/notification-channels/${id}`
}

/**
 * Returns details for a single notification channel.
 * @summary Get notification channel
 */
export const getNotificationChannel = async (
  id: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<NotificationChannelResponse> => {
  return ooreRequest<NotificationChannelResponse>(
    getGetNotificationChannelUrl(id),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getUpdateNotificationChannelUrl = (id: string) => {
  return `/v1/settings/notification-channels/${id}`
}

/**
 * Updates an existing notification channel. Omitted fields are preserved.
 * @summary Update notification channel
 */
export const updateNotificationChannel = async (
  id: string,
  updateNotificationChannelRequest: UpdateNotificationChannelRequest,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<NotificationChannelResponse> => {
  return ooreRequest<NotificationChannelResponse>(
    getUpdateNotificationChannelUrl(id),
    {
      ...options,
      method: 'PUT',
      headers: { 'Content-Type': 'application/json', ...options?.headers },
      body: JSON.stringify(updateNotificationChannelRequest),
    },
  )
}

export const getDeleteNotificationChannelUrl = (id: string) => {
  return `/v1/settings/notification-channels/${id}`
}

/**
 * Permanently deletes a notification channel and its delivery history.
 * @summary Delete notification channel
 */
export const deleteNotificationChannel = async (
  id: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<DeleteNotificationChannelResponse> => {
  return ooreRequest<DeleteNotificationChannelResponse>(
    getDeleteNotificationChannelUrl(id),
    {
      ...options,
      method: 'DELETE',
    },
  )
}

export const getListNotificationDeliveriesUrl = (id: string) => {
  return `/v1/settings/notification-channels/${id}/deliveries`
}

/**
 * Returns delivery history for a notification channel (most recent 100).
 * @summary List notification deliveries
 */
export const listNotificationDeliveries = async (
  id: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<ListNotificationDeliveriesResponse> => {
  return ooreRequest<ListNotificationDeliveriesResponse>(
    getListNotificationDeliveriesUrl(id),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getTestNotificationChannelUrl = (id: string) => {
  return `/v1/settings/notification-channels/${id}/test`
}

/**
 * Sends a test notification to verify the channel is configured correctly.
 * @summary Test notification channel
 */
export const testNotificationChannel = async (
  id: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<TestNotificationChannelResponse> => {
  return ooreRequest<TestNotificationChannelResponse>(
    getTestNotificationChannelUrl(id),
    {
      ...options,
      method: 'POST',
    },
  )
}
