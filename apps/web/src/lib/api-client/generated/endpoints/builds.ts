/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Oore CI API
 * REST API for Oore CI — a self-hosted, Flutter-first mobile CI and internal app distribution platform.
 *
 * The backend daemon (`oored`) exposes this API on the configured listen address. All endpoints under `/v1/` use JSON request/response bodies unless noted otherwise.
 *
 * ## Authentication
 *
 * - **Setup endpoints** (`/v1/setup/*`) are token-gated by a bootstrap session token and auto-disabled after setup completes.
 * - **Auth endpoints** (`/v1/auth/*`) support Local Only login, single-use local recovery in Ready External Access mode, and configured OIDC or trusted-proxy flows.
 * - **Protected API endpoints** declare their session, API, or runner bearer-token requirement individually.
 * - **Callbacks, webhooks, metrics, readiness, and signed-token delivery routes** use their route-specific public, state, signature, or token contract instead of a global bearer requirement.
 *
 * ## Base URL
 *
 * Since Oore CI is self-hosted, the base URL is your daemon's listen address (e.g. `http://localhost:8787`).
 * OpenAPI spec version: 1.0.0
 */
import type {
  BuildChangelogPreviewResponse,
  BuildDetailResponse,
  CancelBuildResponse,
  CreateBuildRequest,
  CreateBuildResponse,
  ListBuildsParams,
  ListBuildsResponse,
  PreviewBuildChangelogParams,
  RerunBuildResponse,
} from '../models'

import { ooreRequest } from '../../transport.ts'

export const getListBuildsUrl = (params?: ListBuildsParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/v1/builds?${stringifiedParams}`
    : `/v1/builds`
}

/**
 * Returns builds, optionally filtered by project, pipeline, or status.
 * @summary List builds
 */
export const listBuilds = async (
  params?: ListBuildsParams,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<ListBuildsResponse> => {
  return ooreRequest<ListBuildsResponse>(getListBuildsUrl(params), {
    ...options,
    method: 'GET',
  })
}

export const getGetBuildUrl = (buildId: string) => {
  return `/v1/builds/${buildId}`
}

/**
 * @summary Get build detail
 */
export const getBuild = async (
  buildId: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<BuildDetailResponse> => {
  return ooreRequest<BuildDetailResponse>(getGetBuildUrl(buildId), {
    ...options,
    method: 'GET',
  })
}

export const getCancelBuildUrl = (buildId: string) => {
  return `/v1/builds/${buildId}/cancel`
}

/**
 * Cancels a queued or running build.
 * @summary Cancel build
 */
export const cancelBuild = async (
  buildId: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<CancelBuildResponse> => {
  return ooreRequest<CancelBuildResponse>(getCancelBuildUrl(buildId), {
    ...options,
    method: 'POST',
  })
}

export const getRerunBuildUrl = (buildId: string) => {
  return `/v1/builds/${buildId}/rerun`
}

/**
 * Creates a new build cloning the original build's config_snapshot, branch, and commit_sha.
 * @summary Re-run build
 */
export const rerunBuild = async (
  buildId: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<RerunBuildResponse> => {
  return ooreRequest<RerunBuildResponse>(getRerunBuildUrl(buildId), {
    ...options,
    method: 'POST',
  })
}

export const getCreateBuildUrl = (projectId: string) => {
  return `/v1/projects/${projectId}/builds`
}

/**
 * Queues a new build for the specified project.
 * @summary Create build
 */
export const createBuild = async (
  projectId: string,
  createBuildRequest: CreateBuildRequest,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<CreateBuildResponse> => {
  return ooreRequest<CreateBuildResponse>(getCreateBuildUrl(projectId), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(createBuildRequest),
  })
}

export const getPreviewBuildChangelogUrl = (
  projectId: string,
  params: PreviewBuildChangelogParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/v1/projects/${projectId}/builds/changelog-preview?${stringifiedParams}`
    : `/v1/projects/${projectId}/builds/changelog-preview`
}

/**
 * @summary Preview a Markdown changelog for a manual build.
 */
export const previewBuildChangelog = async (
  projectId: string,
  params: PreviewBuildChangelogParams,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<BuildChangelogPreviewResponse> => {
  return ooreRequest<BuildChangelogPreviewResponse>(
    getPreviewBuildChangelogUrl(projectId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}
