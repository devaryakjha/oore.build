/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Oore CI API
 * REST API for Oore CI — a self-hosted, Flutter-first mobile CI and internal app distribution platform.
 *
 * The backend daemon (`oored`) exposes this API on the configured listen address. All endpoints under `/v1/` use JSON request/response bodies unless noted otherwise.
 *
 * ## Authentication
 *
 * - **Setup endpoints** (`/v1/setup/*`) are token-gated by a bootstrap session token and auto-disabled after setup completes.
 * - **Auth endpoints** (`/v1/auth/*`) support Local Only login, single-use local recovery in Ready External Access mode, and configured OIDC or trusted-proxy flows.
 * - **Protected API endpoints** declare their session, API, or runner bearer-token requirement individually.
 * - **Callbacks, webhooks, metrics, readiness, and signed-token delivery routes** use their route-specific public, state, signature, or token contract instead of a global bearer requirement.
 *
 * ## Base URL
 *
 * Since Oore CI is self-hosted, the base URL is your daemon's listen address (e.g. `http://localhost:8787`).
 * OpenAPI spec version: 1.0.0
 */
import type {
  CreateScopedDownloadTokenRequest,
  CreateScopedDownloadTokenResponse,
  ListArtifactDownloadTokensResponse,
  RevokeArtifactDownloadTokenResponse,
} from '../models'

import { ooreRequest } from '../../transport.ts'

export const getDownloadViaScopedTokenUrl = (token: string) => {
  return `/install/artifact/${token}`
}

/**
 * Downloads an artifact using a scoped download token. No session auth required —
 * the token itself is the authorization. For S3/R2 storage, redirects to a presigned URL.
 * @summary Download via scoped token
 */
export const downloadViaScopedToken = async (
  token: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<unknown> => {
  return ooreRequest<unknown>(getDownloadViaScopedTokenUrl(token), {
    ...options,
    method: 'GET',
  })
}

export const getRevokeScopedDownloadTokenUrl = (tokenId: string) => {
  return `/v1/artifact-tokens/${tokenId}`
}

/**
 * Revokes a scoped download token so it can no longer be used.
 * @summary Revoke scoped download token
 */
export const revokeScopedDownloadToken = async (
  tokenId: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<RevokeArtifactDownloadTokenResponse> => {
  return ooreRequest<RevokeArtifactDownloadTokenResponse>(
    getRevokeScopedDownloadTokenUrl(tokenId),
    {
      ...options,
      method: 'DELETE',
    },
  )
}

export const getDownloadViaScopedTokenV1Url = (token: string) => {
  return `/v1/artifacts/dl/${token}`
}

/**
 * Resolves a time-limited path token and redirects to the backing storage
 * URL. The token is the authorization; no session bearer token is used.
 * @summary Download via scoped token from the versioned route
 */
export const downloadViaScopedTokenV1 = async (
  token: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<unknown> => {
  return ooreRequest<unknown>(getDownloadViaScopedTokenV1Url(token), {
    ...options,
    method: 'GET',
  })
}

export const getCreateScopedDownloadTokenUrl = (artifactId: string) => {
  return `/v1/artifacts/${artifactId}/scoped-token`
}

/**
 * Generates a scoped, time-limited download token for a specific artifact.
 * The token can be shared with external users who don't have an account.
 * @summary Create scoped download token
 */
export const createScopedDownloadToken = async (
  artifactId: string,
  createScopedDownloadTokenRequest: CreateScopedDownloadTokenRequest,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<CreateScopedDownloadTokenResponse> => {
  return ooreRequest<CreateScopedDownloadTokenResponse>(
    getCreateScopedDownloadTokenUrl(artifactId),
    {
      ...options,
      method: 'POST',
      headers: { 'Content-Type': 'application/json', ...options?.headers },
      body: JSON.stringify(createScopedDownloadTokenRequest),
    },
  )
}

export const getListScopedDownloadTokensUrl = (artifactId: string) => {
  return `/v1/artifacts/${artifactId}/scoped-tokens`
}

/**
 * Lists all scoped download tokens for a specific artifact, including expired and revoked ones.
 * @summary List scoped download tokens
 */
export const listScopedDownloadTokens = async (
  artifactId: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<ListArtifactDownloadTokensResponse> => {
  return ooreRequest<ListArtifactDownloadTokensResponse>(
    getListScopedDownloadTokensUrl(artifactId),
    {
      ...options,
      method: 'GET',
    },
  )
}
