/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Oore CI API
 * REST API for Oore CI — a self-hosted, Flutter-first mobile CI and internal app distribution platform.
 *
 * The backend daemon (`oored`) exposes this API on the configured listen address. All endpoints under `/v1/` use JSON request/response bodies unless noted otherwise.
 *
 * ## Authentication
 *
 * - **Setup endpoints** (`/v1/setup/*`) are token-gated by a bootstrap session token and auto-disabled after setup completes.
 * - **Auth endpoints** (`/v1/auth/*`) support Local Only login, single-use local recovery in Ready External Access mode, and configured OIDC or trusted-proxy flows.
 * - **Protected API endpoints** declare their session, API, or runner bearer-token requirement individually.
 * - **Callbacks, webhooks, metrics, readiness, and signed-token delivery routes** use their route-specific public, state, signature, or token contract instead of a global bearer requirement.
 *
 * ## Base URL
 *
 * Since Oore CI is self-hosted, the base URL is your daemon's listen address (e.g. `http://localhost:8787`).
 * OpenAPI spec version: 1.0.0
 */
import type {
  AppendBuildLogsRequest,
  AppendBuildLogsResponse,
  BuildLogsResponse,
  GetBuildLogsParams,
  StreamBuildLogsParams,
  StreamTokenResponse,
} from '../models'

import { ooreRequest } from '../../transport.ts'

export const getGetBuildLogsUrl = (
  buildId: string,
  params?: GetBuildLogsParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/v1/builds/${buildId}/logs?${stringifiedParams}`
    : `/v1/builds/${buildId}/logs`
}

/**
 * Returns paginated historical build logs.
 * @summary Get build logs
 */
export const getBuildLogs = async (
  buildId: string,
  params?: GetBuildLogsParams,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<BuildLogsResponse> => {
  return ooreRequest<BuildLogsResponse>(getGetBuildLogsUrl(buildId, params), {
    ...options,
    method: 'GET',
  })
}

export const getStreamBuildLogsUrl = (
  buildId: string,
  params?: StreamBuildLogsParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/v1/builds/${buildId}/logs/stream?${stringifiedParams}`
    : `/v1/builds/${buildId}/logs/stream`
}

/**
 * Server-sent event stream of build logs. Requires a short-lived streaming
 * token obtained from `POST /v1/builds/{build_id}/stream-token`.
 * @summary Stream build logs (SSE)
 */
export const streamBuildLogs = async (
  buildId: string,
  params?: StreamBuildLogsParams,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<string> => {
  return ooreRequest<string>(getStreamBuildLogsUrl(buildId, params), {
    ...options,
    method: 'GET',
  })
}

export const getCreateStreamTokenUrl = (buildId: string) => {
  return `/v1/builds/${buildId}/stream-token`
}

/**
 * Exchanges a session token for a short-lived (5 min) streaming token
 * suitable for use in SSE query parameters.
 * @summary Create stream token
 */
export const createStreamToken = async (
  buildId: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<StreamTokenResponse> => {
  return ooreRequest<StreamTokenResponse>(getCreateStreamTokenUrl(buildId), {
    ...options,
    method: 'POST',
  })
}

export const getAppendBuildLogsUrl = (runnerId: string, jobId: string) => {
  return `/v1/runners/${runnerId}/jobs/${jobId}/logs`
}

/**
 * Runner appends log chunks to a build. Max 10,000 lines per build, 4KB per line.
 * @summary Append build logs
 */
export const appendBuildLogs = async (
  runnerId: string,
  jobId: string,
  appendBuildLogsRequest: AppendBuildLogsRequest,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<AppendBuildLogsResponse> => {
  return ooreRequest<AppendBuildLogsResponse>(
    getAppendBuildLogsUrl(runnerId, jobId),
    {
      ...options,
      method: 'POST',
      headers: { 'Content-Type': 'application/json', ...options?.headers },
      body: JSON.stringify(appendBuildLogsRequest),
    },
  )
}
