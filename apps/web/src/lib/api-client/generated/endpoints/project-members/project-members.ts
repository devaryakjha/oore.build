/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Oore CI API
 * REST API for Oore CI — a self-hosted, Flutter-first mobile CI and internal app distribution platform.
 *
 * The backend daemon (`oored`) exposes this API on the configured listen address. All endpoints under `/v1/` use JSON request/response bodies unless noted otherwise.
 *
 * ## Authentication
 *
 * - **Setup endpoints** (`/v1/setup/*`) are token-gated by a bootstrap session token and auto-disabled after setup completes.
 * - **Auth endpoints** (`/v1/auth/*`) support Local Only login, single-use local recovery in Ready External Access mode, and configured OIDC or trusted-proxy flows.
 * - **Protected API endpoints** declare their session, API, or runner bearer-token requirement individually.
 * - **Callbacks, webhooks, metrics, readiness, and signed-token delivery routes** use their route-specific public, state, signature, or token contract instead of a global bearer requirement.
 *
 * ## Base URL
 *
 * Since Oore CI is self-hosted, the base URL is your daemon's listen address (e.g. `http://localhost:8787`).
 * OpenAPI spec version: 1.0.0
 */
import type {
  AddProjectMemberRequest,
  AddProjectMemberResponse,
  ListProjectMemberCandidatesResponse,
  ListProjectMembersResponse,
  UpdateProjectMemberRequest,
  UpdateProjectMemberResponse,
} from '../../models'

import { ooreRequest } from '../../../transport.ts'

export const getListProjectMembersUrl = (projectId: string) => {
  return `/v1/projects/${projectId}/members`
}

/**
 * @summary List project members
 */
export const listProjectMembers = async (
  projectId: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<ListProjectMembersResponse> => {
  return ooreRequest<ListProjectMembersResponse>(
    getListProjectMembersUrl(projectId),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getAddProjectMemberUrl = (projectId: string) => {
  return `/v1/projects/${projectId}/members`
}

/**
 * @summary Add a member to a project
 */
export const addProjectMember = async (
  projectId: string,
  addProjectMemberRequest: AddProjectMemberRequest,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<AddProjectMemberResponse> => {
  return ooreRequest<AddProjectMemberResponse>(
    getAddProjectMemberUrl(projectId),
    {
      ...options,
      method: 'POST',
      headers: { 'Content-Type': 'application/json', ...options?.headers },
      body: JSON.stringify(addProjectMemberRequest),
    },
  )
}

export const getListProjectMemberCandidatesUrl = (projectId: string) => {
  return `/v1/projects/${projectId}/members/candidates`
}

/**
 * @summary List eligible project member candidates
 */
export const listProjectMemberCandidates = async (
  projectId: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<ListProjectMemberCandidatesResponse> => {
  return ooreRequest<ListProjectMemberCandidatesResponse>(
    getListProjectMemberCandidatesUrl(projectId),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getRemoveProjectMemberUrl = (
  projectId: string,
  userId: string,
) => {
  return `/v1/projects/${projectId}/members/${userId}`
}

/**
 * @summary Remove a member from a project
 */
export const removeProjectMember = async (
  projectId: string,
  userId: string,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<void> => {
  return ooreRequest<void>(getRemoveProjectMemberUrl(projectId, userId), {
    ...options,
    method: 'DELETE',
  })
}

export const getUpdateProjectMemberUrl = (
  projectId: string,
  userId: string,
) => {
  return `/v1/projects/${projectId}/members/${userId}`
}

/**
 * @summary Update a project member's role
 */
export const updateProjectMember = async (
  projectId: string,
  userId: string,
  updateProjectMemberRequest: UpdateProjectMemberRequest,
  options?: Parameters<typeof ooreRequest>[1],
): Promise<UpdateProjectMemberResponse> => {
  return ooreRequest<UpdateProjectMemberResponse>(
    getUpdateProjectMemberUrl(projectId, userId),
    {
      ...options,
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json', ...options?.headers },
      body: JSON.stringify(updateProjectMemberRequest),
    },
  )
}
