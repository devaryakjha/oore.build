# Inter font source

`Inter-Regular.ttf` and `Inter-Bold.ttf` come from the official
[Inter 4.1 release](https://github.com/rsms/inter/releases/tag/v4.1).

They are committed so `tools/generate-og-images.tsx` produces deterministic
Satori output locally and in CI. See `OFL.txt` for the license.
